import { test, expect } from '@playwright/test';
import PdfHelpers from './pdfHelpers';

test('homepage has Playwright in title and get started link linking to the intro page', async ({ page }) => {
  const helper = new PdfHelpers();
});
